"""Compliance core: license-rule enforcement, CASL, PIPEDA, and provenance.

This package is the product's moat (spec §2.1). Treat it as load-bearing: data access and
outreach flow through it, and the defaults are deny-by-default.
"""
